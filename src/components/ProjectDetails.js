import React from 'react';

export default ({ project }) => (
    <React.Fragment>
        <h2>Alguns detalhes</h2>
        <dl>
            <dt>Titulo do commit</dt>
            <dd>Descricao do commit</dd>

            <dt>Titulo do commit</dt>
            <dd>Descricao do commit</dd>
        </dl>
    </React.Fragment>
);
